# BMRAG 专业级改进计划

> 目标：将项目从"课程作业级"提升到"面试可展示级"，体现 AI 智能体开发工程师在 RAG 领域的专业性。
> 原则：全面、简约、可快速实现（每项 1-3 小时）、易于面试中讲解。

---

## 一、现状分析

### 已有能力
- [x] 基础 RAG 管线：检索 → 重排序 → 生成
- [x] ChromaDB 向量存储
- [x] CrossEncoder 重排序
- [x] SSE 流式输出
- [x] React 前端 + Docker 部署

### 核心缺失（面试扣分项）
- [ ] **无量化评估**：不知道 RAG 效果好坏，无法量化对比优化
- [ ] **无文档摄入管线**：chunking 策略不可见，无法调整
- [ ] **无查询优化**：直接用原始 query 检索，未做改写/扩展
- [ ] **无混合检索**：仅稠密向量，缺少稀疏检索（BM25）
- [ ] **无可观测性**：无法追踪每次请求的检索结果和性能
- [ ] **无引用溯源**：回答不标注来源，可信度低
- [ ] **Prompt 过于简单**：无结构化设计、无 Few-shot

---

## 二、改进计划（按优先级排序）

### P0 - RAG 评估体系（最核心，面试必问）

> **面试价值**：体现"我知道我的 RAG 效果如何"，而非"我跑通了就行"。

#### 1.1 测试集构建
- 从 `bm.md` 手工构建 20-30 个 Q&A pair 作为 ground truth
- 每条包含：问题、标准答案、相关文档段落标识
- 存为 `tests/qa_dataset.json`

#### 1.2 检索评估指标
- **Recall@K**：Top-K 结果中包含正确文档的比例
- **MRR (Mean Reciprocal Rank)**：正确文档的排名倒数
- **NDCG@K**：考虑排名位置的归一化增益

#### 1.3 生成评估指标
- **Faithfulness（忠实度）**：回答是否基于检索内容，而非编造
- **Answer Relevancy（相关性）**：回答与问题的相关程度
- **Context Precision（上下文精度）**：检索内容中与回答相关的比例
- 实现方式：用 LLM-as-Judge（DeepSeek）自动评分

#### 1.4 效率指标
- 检索延迟（ms）、重排延迟（ms）、生成延迟（ms）、端到端延迟（ms）
- Token 消耗统计

#### 1.5 评估脚本
- `evaluate.py`：一键运行全部评估，输出 Markdown 报告
- 支持对比不同参数（top_k、chunk_size、模型等）的效果

**关键概念**：Recall@K、MRR、NDCG、Faithfulness、LLM-as-Judge
**预计工时**：2-3 小时

---

### P1 - 文档处理与分块策略

> **面试价值**：体现对 chunking 影响检索效果的理解，能讲出不同策略的 trade-off。

#### 2.1 Markdown 感知分块
- 按 `#`/`##`/`###` 标题层级分块
- 保留章节路径作为元数据（如 "第三章 > 第十二条"）
- 保留章节标题文本拼接到 chunk 前面，增强语义

#### 2.2 语义分块（Semantic Chunking）
- 按句子/段落边界切分（而非固定字符数）
- 支持 chunk_overlap（重叠窗口，避免语义断裂）
- 可配置 chunk_size 和 chunk_overlap

#### 2.3 文档摄入管线
- `ingest.py`：统一的文档摄入脚本
- 支持 `.md`、`.txt`、`.pdf`（PDF 用 PyMuPDF）
- 每 chunk 携带元数据：`{source, section, chunk_id, char_range}`
- 支持增量摄入（不重复索引已有内容）

#### 2.4 分块策略对比
- 在评估体系中对比不同 chunk_size（128/256/512）的效果
- 输出对比表格，用数据说话

**关键概念**：Semantic Chunking、Chunk Overlap、Metadata Enrichment
**预计工时**：1.5-2 小时

---

### P1 - 查询优化与混合检索

> **面试价值**：体现对检索阶段优化的深入理解，这是 RAG 效果提升的关键。

#### 3.1 查询改写（Query Rewriting）
- 用 LLM 将用户口语化问题改写为更利于检索的关键词形式
- 示例："手机能拍保密文件吗" → "手机 拍摄 保密文件 规定 禁止"
- 支持多查询生成（Multi-Query）：生成 3 个变体查询，分别检索后合并去重

#### 3.2 混合检索（Hybrid Search）
- 稠密检索：现有 embedding 向量检索（语义匹配）
- 稀疏检索：BM25 关键词检索（精确匹配）
- 融合策略：Reciprocal Rank Fusion (RRF) 加权合并两路结果
- ChromaDB 已支持，或用 `rank_bm25` 库

#### 3.3 上下文组装优化
- 动态计算 token 数，确保不超过 LLM 上下文窗口
- 按重排分数排序填充，优先放最相关内容
- 加入系统指令和 Few-shot 示例

**关键概念**：Query Rewriting、Multi-Query Retrieval、Hybrid Search、RRF
**预计工时**：2 小时

---

### P1 - Prompt 工程与引用溯源

> **面试价值**：Prompt 设计能力是 AI 智能体工程师的基本功。

#### 4.1 结构化 Prompt 模板
```
[角色] 你是一位保密法规专家助手。
[任务] 根据下方检索到的法规片段，回答用户问题。
[约束]
1. 仅基于【检索片段】回答，不得编造
2. 如果片段中没有相关信息，回答"根据现有资料无法回答"
3. 引用片段时标注来源，格式：[来源: 章节名]
[检索片段]
{chunks_with_metadata}
[用户问题]
{query}
```

#### 4.2 引用溯源
- 检索结果携带元数据（章节、来源）
- Prompt 中要求 LLM 标注引用来源
- 前端渲染引用标记，支持点击查看原文

#### 4.3 兜底策略
- 当检索分数全部低于阈值时，提示"未找到相关内容"
- 当生成内容与检索内容重合度低时，添加"此回答可能不准确"提示

**关键概念**：Prompt Template、Chain-of-Thought、Grounding、Citation
**预计工时**：1 小时

---

### P2 - 可观测性与工程化

> **面试价值**：体现工程素养，能排查问题、持续优化。

#### 5.1 结构化日志
- 每次请求记录：query、检索结果（含分数）、重排结果、生成内容、各阶段耗时
- JSON 格式输出，便于后续分析
- 用 `structlog` 或标准 `logging` + JSON formatter

#### 5.2 请求追踪
- 为每次请求生成 `trace_id`
- 前端可查看本次回答的检索详情（命中了哪些 chunk、分数多少）
- SSE 流中在回答结束后附带 metadata 事件

#### 5.3 配置管理
- `config.yaml` 集中管理所有参数
- 模型路径、top_k、chunk_size、prompt 模板等
- 支持环境变量覆盖

#### 5.4 API 文档
- 用 Flask-RESTX 或 flask-smorest 生成 OpenAPI 文档
- 访问 `/docs` 查看 Swagger UI

**关键概念**：Structured Logging、Request Tracing、OpenAPI、Configuration as Code
**预计工时**：1.5 小时

---

### P2 - 前端专业化

> **面试价值**：让项目视觉上更有说服力。

#### 6.1 引用溯源展示
- 回答下方展示"引用来源"卡片
- 点击可展开查看原文 chunk

#### 6.2 检索详情面板
- 侧边可展开"检索详情"
- 展示命中的 chunk 列表、相似度分数、重排分数

#### 6.3 性能指标展示
- 回答完成后显示：检索耗时、生成耗时、Token 数

**预计工时**：1.5 小时

---

### P3 - 高级特性（时间充裕时实现）

#### 7.1 多轮对话
- 维护对话历史，支持上下文追问
- 用 LLM 判断是否需要结合历史信息改写当前问题

#### 7.2 查询路由（Query Routing）
- 判断问题类型：法规查询 / 概念解释 / 闲聊
- 闲聊不走 RAG，直接回答；法规查询走完整管线

#### 7.3 自我反思（Self-Reflection）
- 生成后用 LLM 评估回答质量
- 质量低时自动重新检索或调整参数

#### 7.4 多模态支持
- 从 PDF 中提取表格，转为结构化文本入库
- 从文档中提取图片，用 Vision 模型生成描述后入库

#### 7.5 启动优化（模型加载加速）
**问题**：gunicorn 多 worker 模式下，每个 worker 独立加载模型，启动慢且首次请求慢
**方案**：
- 减少 worker 数量（1 worker + 更多 threads），缩短启动时间
- 增加 `/warmup` 接口，容器启动后主动触发一次推理预热
- K8s 中配置 livenessProbe 调用 warmup，确保服务就绪后再暴露端口
```python
@app.route("/warmup")
def warmup():
    backend.embed_chunk("warmup")
    return jsonify({"status": "warmed"})
```

**面试讲解**："AI 服务冷启动是工程痛点，我通过单 worker + 预热接口的方案，将首次请求延迟从 30s 降到 1s 以内"

**预计工时**：0.5 小时

---

## 三、实施路线图

```
阶段 1（基础专业化）  ────────────────────────────────────────
  ├─ P0: 评估体系      [2-3h]  ← 先建评估，后续优化才有数据支撑
  ├─ P1: 文档分块      [1.5h]  ← 改善数据基础
  └─ P1: Prompt+引用   [1h]    ← 快速提升生成质量

阶段 2（检索优化）    ────────────────────────────────────────
  ├─ P1: 查询改写      [1h]
  ├─ P1: 混合检索      [1h]
  └─ P1: 上下文组装    [0.5h]

阶段 3（工程化）      ────────────────────────────────────────
  ├─ P2: 结构化日志    [0.5h]
  ├─ P2: 配置管理      [0.5h]
  ├─ P2: API 文档      [0.5h]
  └─ P2: 前端引用展示  [1.5h]

阶段 4（高级特性）    ────────────────────────────────────────
  ├─ P3: 按时间余量选做
  └─ P3: 启动优化      [0.5h]  ← 减少 worker + 预热接口
```

---

## 四、改进后的目录结构（预期）

```
bmrag/
├── bmrag_backend/
│   ├── app.py                  # Flask API（增加 /docs, /evaluate 接口）
│   ├── backend.py              # RAG 核心管线（重构为模块化）
│   ├── chunking.py             # 分块策略（Markdown感知 + 语义分块）
│   ├── retrieval.py            # 检索模块（混合检索 + 查询改写）
│   ├── evaluate.py             # 评估脚本（一键运行全部指标）
│   ├── config.yaml             # 集中配置
│   ├── ingest.py               # 文档摄入脚本
│   ├── prompts.py              # Prompt 模板管理
│   ├── requirements.txt
│   ├── Dockerfile
│   └── ...
├── tests/
│   ├── qa_dataset.json         # 评估测试集（20-30 条）
│   └── test_retrieval.py       # 单元测试
├── bmrag_front/
│   └── ...（React + TS，增加引用展示）
├── docs/
│   └── evaluation_report.md    # 评估报告
├── docker-compose.yaml
└── plan.md                     # 本文件
```

---

## 五、面试讲解要点

每个改进点在面试中可以这样讲：

| 改进点 | 一句话讲解 |
|--------|-----------|
| 评估体系 | "我用 Recall@K 和 Faithfulness 量化评估 RAG 效果，而不是凭感觉调参" |
| 分块策略 | "我对比了固定长度和 Markdown 感知分块，后者在法规类文档上 Recall 提升了 X%" |
| 查询改写 | "用户口语化提问检索效果差，我用 LLM 改写为关键词形式，Recall 提升了 X%" |
| 混合检索 | "纯向量检索对专有名词不敏感，我用 BM25 + 向量的混合检索，兼顾精确和语义匹配" |
| 引用溯源 | "每个回答都标注引用来源，用户可溯源验证，增强可信度" |
| 可观测性 | "每次请求都有 trace_id，可以追踪检索结果和各阶段耗时，便于定位问题" |
| Prompt 工程 | "我设计了结构化 Prompt，包含角色、约束、Few-shot，降低了幻觉率" |

---

## 六、评估指标目标值

| 指标 | 当前（预估） | 目标 |
|------|-------------|------|
| Recall@5 | 未知 | > 0.85 |
| MRR | 未知 | > 0.65 |
| Faithfulness | 未知 | > 0.80 |
| 端到端延迟 | 未知 | < 3s（首 token < 1s） |

> 改进第一步就是跑评估拿到当前基线数据，后续每次优化都能看到数值变化。
